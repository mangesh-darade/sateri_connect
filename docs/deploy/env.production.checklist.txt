# Production .env checklist (copy values into server `.env` — never commit `.env`)

CI_ENVIRONMENT = production

app.baseURL = 'https://androidtestings.elintpos.in/'
app.forceGlobalSecureRequests = true
app.indexPage = 'index.php'

database.default.hostname = localhost
database.default.database = YOUR_DB
database.default.username = YOUR_USER
database.default.password = YOUR_PASS
database.default.DBDriver = MySQLi
database.default.port = 3306
database.default.charset = utf8mb4
database.default.DBCollat = utf8mb4_general_ci

# php spark key:generate
encryption.key =

# Long random string (required in production — do not reuse encryption.key)
JWT_SECRET =

# After upload to project-root DocumentRoot (Plesk / androidtestings.elintpos.in):
#   1. Upload code (public/assets is in git; root /assets is NOT — gitignored)
#   2. On server run migrations (required for chat inbox columns):
#        php spark migrate
#   3. On server run:  php spark webroot:publish --force
#      (or open any page once — root index.php auto-copies assets when CSS is missing/stale)
#   4. Verify: https://androidtestings.elintpos.in/assets/css/app.css
#      Hard-refresh browser (Ctrl+F5) after deploy — CDN/browser cache keeps old CSS.
#
# If CSS still broken: check app.baseURL has trailing slash and matches the host,
# and that indexPage = 'index.php' when rewrite rules are not enabled.
#
# If /chat/conversations returns 500: run `php spark migrate` then check writable/logs.
